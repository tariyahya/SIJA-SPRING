# Backend Docs Placeholder

Tempatkan artefak pendukung backend pada folder ini:

- `ERD.png` – diagram relasi database.
- `flow-presensi-rfid.png` – flow presensi RFID.
- `flow-presensi-geolocation.png` – flow validasi lokasi.
- `postman-collection.json` – koleksi request untuk demo.

Gunakan file Markdown tambahan bila perlu untuk menjelaskan konfigurasi, asumsi, atau catatan per tahap.
